# Copy to .env on the server (.env is gitignored).

# Public domain served by nginx. Used for server_name and the www -> apex redirect.
DOMAIN=autobanali.am

# Image published by .github/workflows/docker-publish.yml.
# Replace OWNER/REPO with the actual GitHub owner/repo, lowercase.
IMAGE=ghcr.io/OWNER/REPO
IMAGE_TAG=latest